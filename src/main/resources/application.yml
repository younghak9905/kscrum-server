server:
    port: 8080
    tomcat:
        access-log:
            enabled: false
            directory: logs
            prefix: access_log
            suffix: .access.log
        basedir: .

spring:
    profiles:
        default: local
    servlet:
        multipart:
            maxFileSize: 100MB
            maxRequestSize: 100MB
